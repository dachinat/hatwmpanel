/*
 * Meson generates the Wayland protocol implementation in the build tree.
 * CGO_CFLAGS adds that directory to the include path for Go builds and tests.
 */
#include "wlr-layer-shell-unstable-v1-protocol.c"
